# See https://fly.io/docs/reference/configuration/ for information about how to use this file.

app = "animeaux-admin-3ac2"
primary_region = "ams"
kill_signal = "SIGINT"
kill_timeout = "5s"
swap_size_mb = 512

[experimental]
  auto_rollback = true

[deploy]
  release_command = "pnpm --filter prisma exec prisma migrate deploy"

[env]
  PORT = "8080"

[[services]]
  protocol = "tcp"
  internal_port = 8080
  processes = ["app"]

  [[services.ports]]
    port = 80
    handlers = ["http"]
    force_https = true

  [[services.ports]]
    port = 443
    handlers = ["tls", "http"]

  [services.concurrency]
    type = "requests"
    hard_limit = 100
    soft_limit = 80

  [[services.http_checks]]
    interval = "1m"
    timeout = "2s"
    grace_period = "1m"
    restart_limit = 0
    method = "get"
    path = "/healthcheck"
    protocol = "http"
