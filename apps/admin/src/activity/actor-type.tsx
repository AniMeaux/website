import { ActivityActorType as PrismaActivityActorType } from "@prisma/client";

export namespace ActivityActorType {
  export const Enum = PrismaActivityActorType;
  export type Enum = PrismaActivityActorType;
}
